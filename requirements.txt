# Core runtime dependencies for AIworker.
# The core engine (generation, guardrails, approval, scheduling, reporting)
# deliberately depends on the Python standard library + PyYAML only, so it can
# run on a free-tier VPS or a laptop with no build toolchain.
PyYAML>=6.0
