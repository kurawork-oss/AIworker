#!/usr/bin/env bash
# Run the CLI without installing the package.
#   ./scripts/aiworker status
set -euo pipefail
here="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
PYTHONPATH="$here/src${PYTHONPATH:+:$PYTHONPATH}" exec python3 -m aiworker "$@"
